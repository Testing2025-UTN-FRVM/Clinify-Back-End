# Variables para pruebas de integracion
NODE_ENV=test

# Valores por defecto (Testcontainers los sobreescribirá)
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=test_user
DB_PASSWORD=test_password
DB_DATABASE=clinify_test

JWT_SECRET=test-secret-key-for-integration-tests
JWT_EXPIRATION=1h
